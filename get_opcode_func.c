#include "monty.h"

/**
 * get_opcode_func - gets the opcode function and returns it
 * @opcode: the opcode string
 *
 * Return: NULL if invalid else function
 */
(f*)(stack_t **stack, unsigned int line_number) get_opcode_func()
{
}
