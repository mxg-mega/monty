#include "monty.h"

/**
 * is_Empty - function that checks if the stack is empty
 * @stack: top/head of the stack
 *
 * Return: 0 if empty and -1 if not
 */
int is_Empty(stack_t *stack)
{
	if (stack == NULL)
	{
		return (0);
	}
	return (-1);
}
